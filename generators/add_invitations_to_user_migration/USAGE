Description:
    The migration generator creates a migration to add invitations limit to user

Example:
    ./script/generate add_invitations_to_user
	
	Migration:  db/migrate/add_invitations_to_user.rb
	
	
  The invitations model lives in the lib directory of this plugin. You can move that model into your models directory and modify it if you like.